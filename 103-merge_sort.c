#include "sort.h"

/**
 * merge_sort - sorts an array of integers in ascending order
 * using the Merge sort algorithm
 * @array: list if elements
 * @size: of array
 *
 * Return: void, nothing
 */

void merge_sort(int *array, size_t size)
{
	(void) array;
	(void) size;
}
